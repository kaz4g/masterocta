name: RC Release Build (disabled)

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  create-release:
    # Public distribution is not authorized. Keep this legacy workflow inert;
    # Gate C candidates use gate-c-candidate.yml and remain draft/non-public.
    if: ${{ false }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      release_id: ${{ steps.create_release.outputs.id }}
      version: ${{ steps.version.outputs.version }}
      version_msi: ${{ steps.version.outputs.version_msi }}
      tag_name: ${{ steps.version.outputs.tag_name }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Get version info
        id: version
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          RC_VERSION="0.0.1-rc"
          RC_VERSION_MSI="0.0.1-0"
          TAG_NAME="v0.0.1-rc"

          echo "version=${RC_VERSION}" >> $GITHUB_OUTPUT
          echo "version_msi=${RC_VERSION_MSI}" >> $GITHUB_OUTPUT
          echo "tag_name=${TAG_NAME}" >> $GITHUB_OUTPUT
          echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "Created RC build (version: ${RC_VERSION}, msi version: ${RC_VERSION_MSI}, tag: ${TAG_NAME}, commit: ${SHORT_SHA})"

      - name: Delete existing release and tag
        run: |
          TAG_NAME="${{ steps.version.outputs.tag_name }}"

          # Delete existing release if it exists
          if gh release view "$TAG_NAME" &>/dev/null; then
            echo "Deleting existing release: $TAG_NAME"
            gh release delete "$TAG_NAME" --yes
          fi

          # Delete existing tag if it exists
          if git ls-remote --tags origin | grep -q "refs/tags/$TAG_NAME"; then
            echo "Deleting existing tag: $TAG_NAME"
            git push origin --delete "$TAG_NAME" || true
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: "Release Candidate ${{ steps.version.outputs.tag_name }}"
          body: |
            ## Release Candidate for Testers

            This is a pre-release build intended for testers.
            This release is automatically overwritten on each RC build.

            - Version: `${{ steps.version.outputs.version }}` (`${{ steps.version.outputs.short_sha }}`)
            - Commit: `${{ github.sha }}`
            - Branch: `${{ github.ref_name }}`
            - Built: `${{ github.event.repository.updated_at }}`

            > This build may be unstable. Please report any issues you find.
          draft: true
          prerelease: true
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-tauri:
    needs: create-release
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            args: '--target x86_64-unknown-linux-gnu'
            rust_targets: ''
          - platform: ubuntu-24.04-arm
            args: '--target aarch64-unknown-linux-gnu'
            rust_targets: ''
          - platform: windows-latest
            args: ''
            rust_targets: ''
          - platform: windows-latest
            args: '--target aarch64-pc-windows-msvc'
            rust_targets: 'aarch64-pc-windows-msvc'
          - platform: macos-latest
            args: '--target x86_64-apple-darwin'
            rust_targets: 'aarch64-apple-darwin,x86_64-apple-darwin'
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
            rust_targets: 'aarch64-apple-darwin,x86_64-apple-darwin'

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Install pnpm and Node.js
        uses: pnpm/setup@c9883cc79df532ad1a7b81bf9ab944ceb090d65c # v2.0.0
        with:
          runtime: node@22
          cache: true
          install: false

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable
        with:
          toolchain: stable
          targets: ${{ matrix.rust_targets }}

      - name: Install dependencies (Ubuntu)
        if: contains(matrix.platform, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev patchelf xdg-utils

      - name: Install frontend dependencies
        run: pnpm --filter masterocta install --frozen-lockfile

      - name: Update version for RC build
        shell: bash
        run: |
          # Use MSI-compatible version (numeric pre-release) on Windows
          if [[ "${{ matrix.platform }}" == "windows-latest" ]]; then
            VERSION="${{ needs.create-release.outputs.version_msi }}"
          else
            VERSION="${{ needs.create-release.outputs.version }}"
          fi
          echo "Setting version to $VERSION"

          # Update Cargo.toml
          sed -i.bak "s/^version = \".*\"/version = \"$VERSION\"/" src-tauri/Cargo.toml

          # Update tauri.conf.json
          sed -i.bak "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" src-tauri/tauri.conf.json

          # Show the changes
          echo "Updated Cargo.toml:"
          grep "^version" src-tauri/Cargo.toml
          echo "Updated tauri.conf.json:"
          grep "\"version\"" src-tauri/tauri.conf.json

      - name: Build and upload artifacts (Ubuntu)
        if: contains(matrix.platform, 'ubuntu')
        uses: tauri-apps/tauri-action@e834788a94591d81e3ae0bd9ec06366f5afb8994 # v0.5.23
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NO_STRIP: 1
        with:
          releaseId: ${{ needs.create-release.outputs.release_id }}
          tagName: ${{ needs.create-release.outputs.tag_name }}
          args: ${{ matrix.args }}

      - name: Build and upload artifacts (Windows/macOS)
        if: ${{ !contains(matrix.platform, 'ubuntu') }}
        uses: tauri-apps/tauri-action@e834788a94591d81e3ae0bd9ec06366f5afb8994 # v0.5.23
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          releaseId: ${{ needs.create-release.outputs.release_id }}
          tagName: ${{ needs.create-release.outputs.tag_name }}
          args: ${{ matrix.args }}

      - name: Retry upload on failure
        if: failure()
        uses: nick-fields/retry@ce71cc2ab81d554ebbe88c79ab5975992d79ba08 # v3.0.2
        with:
          timeout_minutes: 5
          max_attempts: 2
          retry_wait_seconds: 30
          shell: bash
          command: |
            cd src-tauri/target

            # Upload any built artifacts that may have failed
            if [[ "${{ matrix.platform }}" == *"ubuntu"* ]]; then
              find release/bundle -name "*.deb" -o -name "*.AppImage" | xargs -I {} gh release upload "${{ needs.create-release.outputs.tag_name }}" {} --clobber || true
            elif [[ "${{ matrix.platform }}" == "windows-latest" ]]; then
              find release/bundle -name "*.msi" -o -name "*.exe" | xargs -I {} gh release upload "${{ needs.create-release.outputs.tag_name }}" {} --clobber || true
            else
              find . -name "*.dmg" | xargs -I {} gh release upload "${{ needs.create-release.outputs.tag_name }}" {} --clobber || true
            fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Retained only as inert history. Public release publication is disabled.
  publish-release:
    needs: [create-release, build-tauri]
    if: ${{ false }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Publish release (undraft)
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
        with:
          script: |
            await github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: ${{ needs.create-release.outputs.release_id }},
              draft: true,
            });
            core.info('Legacy RC release publishing is disabled');
